// Firebase 제거 - 더 이상 필요 없음
// 빈 파일로 유지 (import 오류 방지)

export const initializeFirebaseApp = async () => {
  // 아무것도 하지 않음
  console.log('로컬 스토리지 모드로 실행 중');
};

export const initializeFirebase = () => {
  // 아무것도 하지 않음
};

export const getDbInstance = () => null;
export const getFunctionsInstance = () => null;
export const getAuthInstance = () => null;
